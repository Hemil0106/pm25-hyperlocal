"""Globalization layer (Milestone 15): AOI-configurable, tile-based platform.

Submodules:
    datasets      - dataset registry (Phase 5)
    ground_truth  - normalized ground-truth PM2.5 (Phase 6)
    availability  - data availability manifest (Phase 7)
    ingestion     - AOI-aware ingestion interfaces (Phase 8)
    cache         - tile cache integration (Phase 9)
"""
